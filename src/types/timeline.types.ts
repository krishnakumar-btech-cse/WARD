import type { CatalystRow } from './catalyst.types';

/** A chronology entry (upload, edit, assignment, AI insight, officer activity) for a case. */
export interface TimelineEventRecord extends CatalystRow {}
