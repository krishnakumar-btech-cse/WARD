import type { CatalystRow } from './catalyst.types';

/** A resolved entity (person, vehicle, device, location, financial) in the criminal network graph. */
export interface NetworkEntityRecord extends CatalystRow {}
