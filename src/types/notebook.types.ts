import type { CatalystRow } from './catalyst.types';

/** A notebook entry (voice note, OCR text, image, PDF, or free text) attached to a case. */
export interface NotebookEntryRecord extends CatalystRow {}
