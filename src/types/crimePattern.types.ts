import type { CatalystRow } from './catalyst.types';

/** A precomputed hotspot/temporal/MO analytics aggregate, written by a scheduled Catalyst Job. */
export interface CrimePatternAggregateRecord extends CatalystRow {}
