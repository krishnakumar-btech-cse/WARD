import type { CatalystRow } from './catalyst.types';

/** An investigator-authored theory for a case, optionally linked to supporting/contradicting evidence. */
export interface CaseHypothesisRecord extends CatalystRow {}
