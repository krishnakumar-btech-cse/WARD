import type { CatalystRow } from './catalyst.types';

/** Officer <-> case assignment. Existing table, real columns discovered via getColumns(). */
export interface CaseAssignmentRecord extends CatalystRow {}
